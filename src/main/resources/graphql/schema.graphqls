type Query {
  movies: [Movie]
  movieById(id: ID!): Movie
}

type Mutation {
  addMovie(title: String!, director: String!, genre: String!, releaseYear: Int!): Movie
}

type Movie {
  id: ID!
  title: String
  director: String
  genre: String
  releaseYear: Int
}
